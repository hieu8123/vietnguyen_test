import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

const InspectionSheet: React.FC = () => {
  return (
    <Card>
      <Title level={3}>Inspection Sheet (F-QC-03-01)</Title>
      <p>Form này đang được phát triển...</p>
    </Card>
  );
};

export default InspectionSheet;