import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

const MaterialInOut: React.FC = () => {
  return (
    <Card>
      <Title level={3}>Bảng theo dõi nhập xuất NVL (F-WH-01-01)</Title>
      <p>Form này đang được phát triển...</p>
    </Card>
  );
};

export default MaterialInOut;