import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

const SuppliesImport: React.FC = () => {
  return (
    <Card>
      <Title level={3}>Phiếu nhập kho phụ liệu, vật tư, văn phòng phẩm (F-WH-10-01)</Title>
      <p>Form này đang được phát triển...</p>
    </Card>
  );
};

export default SuppliesImport;