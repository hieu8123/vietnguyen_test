import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

const MaterialExport: React.FC = () => {
  return (
    <Card>
      <Title level={3}>Phiếu xuất kho NVL (F-WH-09-01)</Title>
      <p>Form này đang được phát triển...</p>
    </Card>
  );
};

export default MaterialExport;